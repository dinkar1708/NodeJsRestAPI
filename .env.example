# Server Configuration
NODE_ENV=development
PORT=3000

# MongoDB Configuration
MONGO_HOST=localhost
MONGO_PORT=27017
MONGO_DB=REST_DEMO

# For local development (change IP as needed)
HOST_IP=127.0.0.1
